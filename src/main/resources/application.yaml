spring:
  config:
    import: "optional:file:.env"
  application:
    name: warehouse_inventory_management
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}
  jpa:
    hibernate:
      ddl-auto: update